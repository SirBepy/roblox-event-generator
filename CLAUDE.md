## Project

Type: other
Deploy: none

## Structure

roblox_event_generator/ (cli.py, generator.py), pyproject.toml for packaging

## Rules

- Python CLI tool, no web frontend
- Packaged via setuptools, installed via pip from git
- Entry point: `roblox-event-generator` CLI command
